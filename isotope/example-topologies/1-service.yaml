defaults:
  fortio_cluster: some_context_string
  ingress_gateway_endpoint: x.x.x.x
  requestSize: 1 KB
  responseSize: 1 KB
services:
- name: a
  cluster_context: context_string
  isEntrypoint: true
